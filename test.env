# RXinDexer Test Configuration
# Generated for production testing

# Radiant Core RPC connection
DAEMON_URL=http://radiantrpc:dbe8d15ab28778e9155c7644f1f36898@localhost:7332/

# Network settings
COIN=Radiant
NET=mainnet

# Database (using leveldb since rocksdb build failed on Python 3.14)
DB_ENGINE=leveldb
DB_DIRECTORY=/Users/main/Documents/Radiant/RXinDexer/electrumx-db-test

# Services (TCP only for testing, no SSL cert needed)
SERVICES=tcp://0.0.0.0:50010,rpc://0.0.0.0:8000

# Performance
REQUEST_TIMEOUT=60
CACHE_MB=1200
MAX_SESSIONS=1000
MAX_SEND=10000000
MAX_RECV=10000000

# Rate limiting
COST_SOFT_LIMIT=1000
COST_HARD_LIMIT=10000

# RXinDexer: Enable all indexing features
GLYPH_INDEX=1
WAVE_INDEX=1
SWAP_INDEX=1
GLYPH_SUBSCRIPTIONS=1
MEMPOOL_GLYPH_INDEX=1
MEMPOOL_SWAP_INDEX=1

# WAVE configuration
WAVE_HOT_NAMES=10000

# Logging
LOG_LEVEL=INFO
