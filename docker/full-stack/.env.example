# RXinDexer Full Stack Configuration
# Copy to .env and customize

# ===========================================
# Radiant Node RPC credentials
# ===========================================
RPC_USER=radiant
RPC_PASS=your_secure_password_here

# ===========================================
# Performance Settings
# ===========================================
CACHE_MB=2000
MAX_SESSIONS=10000
LOG_LEVEL=INFO

# ===========================================
# RXinDexer Indexing Features
# Set to 0 to disable any feature
# ===========================================

# Glyph token indexing (FT, NFT, dMint, Mutable, Containers)
GLYPH_INDEX=1

# WAVE naming system indexing
WAVE_INDEX=1
WAVE_HOT_NAMES=10000

# Swap DEX order book indexing
SWAP_INDEX=1

# Real-time WebSocket subscriptions
GLYPH_SUBSCRIPTIONS=1

# Mempool indexing (unconfirmed transactions)
MEMPOOL_GLYPH_INDEX=1
MEMPOOL_SWAP_INDEX=1

# ===========================================
# REST API Security (required in production)
# ===========================================

# Comma-separated list of allowed CORS origins for the REST API.
# Required when ELECTRUMX_ENV=prod. Use * only for development.
# Example: ALLOWED_ORIGINS=https://yourapp.com,https://api.yourapp.com
ALLOWED_ORIGINS=

# Optional API key to restrict REST API access.
# Leave empty to allow unauthenticated access.
REST_API_KEY=

# ===========================================
# Rate Limiting
# ===========================================
# Relaxed defaults prevent local RPC from being throttled during sync.
COST_SOFT_LIMIT=10000
COST_HARD_LIMIT=100000
INITIAL_CONCURRENT=50
REQUEST_SLEEP=500
