# /Users/radiant/Desktop/RXinDexer/deploy/env.template
# This file serves as a template for generating environment-specific .env files during deployment.
# Variables enclosed in ${VARIABLE} will be replaced with values from the config.<environment>.env files.

# Radiant Node RPC Connection
RADIANT_RPC_URL=${RADIANT_NODE_URL}
RADIANT_RPC_USER=${RADIANT_RPC_USER}
RADIANT_RPC_PASSWORD=${RADIANT_RPC_PASSWORD}
RADIANT_RPC_PORT=${RADIANT_RPC_PORT}

# PostgreSQL Database
DB_HOST=${DB_HOST}
DB_PORT=${DB_PORT}
DB_NAME=${DB_NAME}
DB_USER=${DB_USER}
DB_PASSWORD=${DB_PASSWORD}

# Redis Cache
REDIS_HOST=${REDIS_HOST}
REDIS_PORT=${REDIS_PORT}
REDIS_DB=${REDIS_DB}
REDIS_PASSWORD=${REDIS_PASSWORD}

# API Settings
API_HOST=${API_HOST}
API_PORT=${API_PORT}
API_DEBUG=${API_DEBUG}
API_WORKERS=${API_WORKERS}

# Sync Settings
SYNC_BATCH_SIZE=${SYNC_BATCH_SIZE}
SYNC_START_HEIGHT=${SYNC_START_HEIGHT}

# Logging
LOG_LEVEL=${LOG_LEVEL}
LOG_FILE=${LOG_FILE}
